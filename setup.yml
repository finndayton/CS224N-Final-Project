name: cs224n_dfp1
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.8
  - pytorch
  - torchvision
  - torchaudio
  #- pytorch-cuda
  - pip
  - pip:
    - tqdm
    - requests
    - importlib-metadata
    - filelock
    - scikit-learn
    - tokenizers
    - explainaboard_client
    - datasets
    - numpy
    - sklearn
    - explainaboard_client